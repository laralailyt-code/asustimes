# ASUSTIMES 資料來源與更新頻率報告

> 最後更新：**2026-05-05**
> 維護責任：修改資料來源、更新頻率、背景執行緒或部署啟動方式時，請同步更新本報告。

---

## 目錄

1. [新聞](#1-新聞)
2. [原物料與匯率](#2-原物料與匯率)
3. [風險事件監控](#3-風險事件監控)
4. [Telegram Bot 推播](#4-telegram-bot-推播)
5. [系統架構與部署](#5-系統架構與部署)

---

## 1. 新聞

### 更新頻率

| 機制 | 頻率 | 程式位置 |
|------|------|---------|
| 主背景刷新 | **每 1 小時** | `REFRESH_INTERVAL = 60 * 60`、`background_refresh_loop()` |
| 手動刷新 | 即時啟動背景 thread | `POST /api/refresh` |
| 啟動時刷新 | 背景 loop 啟動後立即跑一次 | `background_refresh_loop()` |
| 前端讀取快取 | 每 15 分鐘重新讀 API | `templates/index.html` `setInterval(...)` |

> 現況：已沒有獨立的 `_digitimes_refresh_loop()`。舊的 2 小時 Digitimes 強化刷新已移除，避免和主刷新重複打 Bing/Digitimes。

### 新聞來源

目前新聞由 `scraper.py` 的 `FEEDS` 統一維護，`fetch_all_news()` 以 4 個 worker 平行抓取，總 timeout 120 秒。

#### A. 直接 RSS Feed

| 來源 | 數量 / URL | 主要內容 |
|------|------------|---------|
| **Digitimes 官方 RSS** | 22 個分類 RSS：`https://www.digitimes.com.tw/tech/rss/xml/...` | 半導體、供應鏈、AI、顯示、伺服器、PC、區域與商情 |
| **科技新報** | `https://technews.tw/feed/` | AI、半導體、產業趨勢 |
| **iThome** | `https://www.ithome.com.tw/rss` | IT 與企業科技 |
| **電腦王** | `https://feeds.feedburner.com/cool3c-all` | 消費電子、PC |

工商時報直接 RSS 目前未使用，程式註解原因為 `ctee.com.tw` 直接 RSS 被 Cloudflare WAF 擋 HTTP 403，改走 Bing News site 搜尋。

#### B. Bing News RSS

`GN` / `GN_EN` 目前都指向：

```text
https://www.bing.com/news/search?format=rss&q=
```

| 類型 | 目前查詢 |
|------|---------|
| Digitimes site 搜尋 | 5 組：PC、伺服器、記憶體、面板、財報 |
| 工商時報 site 搜尋 | 10 組：科技、半導體、記憶體、伺服器、面板、晶片、電子、出口、蘋果、ASUS |
| 台灣科技主題 | 8 組：AI 伺服器、HBM、台積電、電競顯卡、PC 市場、OLED、法說會、財報 |
| Global Tech | 3 組英文查詢：TSMC、NVIDIA、semiconductor earnings |
| 供應鏈風險新聞 | 罷工、戰爭、紅海、颱風、地震/水災等關鍵字 |
| Watchlist 供應商 | 從 `watchlist.csv` 動態取紅燈/黃燈供應商，最多 20 組 |

> 注意：少數 feed 的 `source` label 仍寫成 `Google News`，但實際 URL 來源是 Bing News。Google News URL 目前不在 `FEEDS` 使用，只保留 redirect 解析 helper 以處理舊資料或相容情境。

### 分類標籤

目前 `CATEGORY_KEYWORDS` 有 11 類：

AI 產業 / 記憶體/儲存 / 半導體 / PC / NB / 伺服器/雲端 / 面板/顯示 / 電競/ROG / 供應鏈/關稅 / 財務風險 / 財報/法說 / ESG永續

### AI 摘要與翻譯

| 用途 | 來源 / 模型 | 觸發 |
|------|-------------|------|
| 分類摘要 | Anthropic `claude-sonnet-4-6`，需 `ANTHROPIC_API_KEY` | `/api/digest`，每分類快取 |
| 英文新聞標題翻譯 | `google-translate-py` → `google-trans-new` → Claude Haiku fallback → 保留英文 | RSS 解析時 |

摘要要求為繁中 2-5 條，每條約 50-80 字。Claude 不可用或回傳 `NONE` 時，改用 top articles 的標題與 snippet fallback。

### 持久化

| 檔案 | 說明 |
|------|------|
| `news_archive.json` | 新聞 archive，保留 730 天 |

每次刷新會把 fresh articles 寫入 archive，再載入 2 年內 archive，依 URL 去重後由 fresh 覆蓋 archived。

### 已停用 / 已移除來源

- Google News RSS URL：目前 feed URL 已改 Bing News；只保留舊 Google redirect resolver。
- Digitimes Playwright / login crawler：函式仍在 `scraper.py`，但 `fetch_all_news()` 不呼叫。
- Digitimes 獨立 2 小時刷新 loop：已從 `app.py` 移除。
- Tom's Hardware、Yahoo 財經 / Yahoo 新聞：目前不在 `FEEDS`。

---

## 2. 原物料與匯率

### 更新頻率

| 機制 | 頻率 | 程式位置 |
|------|------|---------|
| 自動刷新 | **每天台北時間 07/09/11/13/15/17 共 6 次** | `_live_price_loop()` |
| loop 啟動刷新 | 讀 CSV 後立即刷新一次 | `_load_commodity_csv_to_cache()` + `_refresh_live_prices()` |
| 本地直接啟動刷新 | `python app.py` 時先同步刷新一次 | `if __name__ == "__main__"` |
| 手動刷新 | 即時啟動背景 thread | `POST /api/commodities/refresh` |

> 現況：repo 內沒有 `.github/workflows/commodity-daily.yml`，因此報告不再列 GitHub Action 雲端備援。

### 價格與匯率來源

| 類別 | 商品 | 目前主要來源 | 備註 |
|------|------|--------------|------|
| 貴金屬 / 能源 / 部分金屬 | 金、銀、WTI、Brent、銅、鋁 | Yahoo Finance / yfinance，失敗 fallback Yahoo v8 HTTP | 銅 `HG=F` 乘 2204.62 轉 USD/tonne；鋁 `ALI=F` |
| LME / 金屬 | 鈷 | 鉅亨網 cnyes LME cobalt cash-settle | 取約 360 天日資料，合併 cache |
| LME / 金屬 | 錫、鎳、鋅 | Trading Economics commodity page | metals.live dedicated block 已從 refresh 流程移除 |
| 電池金屬 | 鋰 | Trading Economics | CNY/tonne |
| 化工 | 黃磷 | SCI99 JSON API `oldId=678` | 失敗時保留既有歷史 |
| 塑膠 | PC 塑料 (SABIC) | SCI99 JSON API `oldId=68` | 失敗 fallback buyplas |
| 塑膠 | PC/ABS (SABIC) | buyplas.com | latest-only，無公開完整歷史 |
| 塑膠 | ABS 聚合物（注塑） | 台灣銀行 BCD API `130041` | 抓歷史序列 |
| 包裝 | 瓦楞芯紙 | 台灣銀行 BCD API `190060` | 抓歷史序列 |
| 包裝 | NOREXECO 長纖紙漿 | MoneyDJ | MoneyDJ 失敗時保留歷史 cache |
| 戰略物資 | 鎢粉 | 上海有色網 SMM | Playwright 優先，requests 解析 fallback |
| 匯率 | TWD、CNY、JPY、EUR、BRL、KRW、IDR、INR | Yahoo Finance / yfinance，失敗 fallback Yahoo v8 HTTP | 皆為「1 USD 對外幣」 |

### 商品總清單

目前 `_COMMODITY_CATEGORIES` 共 **26 項**：

| 分類 | 項目 |
|------|------|
| 金屬 8 | 銅、錫、鋁、鎳、鋅、鈷、鋰、鎢 |
| 貴金屬 2 | 金、銀 |
| 能源 2 | 石油 西德州、石油 北海布蘭特 |
| 原物料 6 | 黃磷、ABS聚合物、PC塑料 (SABIC)、PC/ABS塑料、NOREXECO 長纖紙漿、瓦楞芯紙 |
| 匯率 8 | USD/TWD、USD/CNY、USD/JPY、USD/EUR、USD/BRL、USD/KRW、USD/IDR、USD/INR |

### Commodity News

`/api/commodity-news` 是原物料新聞查詢，不是價格來源。

| 來源 | 用途 | 節流 / 快取 |
|------|------|-------------|
| mining.com | 英文 commodity news pool | 全站共用 60 分鐘 cache |
| GDELT 2.0 Doc API | 補英文新聞不足 | 每 host 約 6 秒以上間隔 |
| Bing News RSS | 中文「商品名 價格」查詢 | per commodity 60 分鐘 cache、每日 200 query cap、429 後 60 分鐘 cooldown |
| Google Translate gtx / MyMemory | 英文標題翻中 | 翻譯 cache |

### Carry-forward 機制

CSV 寫入 `2026 Raw material trend history.csv` 前會執行 `_apply_carry_forward()`：

| 規則 | 說明 |
|------|------|
| 向前沿用 | 空白格用最近一筆真實值沿用，標 `*` |
| 向後回填 | 第一筆真實點前的空白，最多只回填近 30 天 |
| 真實值寫入 | 今日已存在的 cache 條目會先移除，再由 fresh source 覆蓋 |
| 目的 | 避免圖表長期空白，同時降低舊歷史被污染的風險 |

---

## 3. 風險事件監控

### 更新頻率

| 機制 | 頻率 | 程式位置 |
|------|------|---------|
| 罷工 + 地緣政治預熱 | **每 3 小時** | `_risk_cache_preload_loop()` |
| 災害事件落地 Supabase | 首輪 30 秒後，之後 **每 90 秒** | `_disaster_persist_loop()` |
| USGS 地震 proxy | 5 分鐘 cache | `/api/risk/quakes` |
| NOAA 風暴 proxy | 5 分鐘 cache | `/api/risk/storms` |
| GDACS 洪水/火山 proxy | 5 分鐘 cache | `/api/risk/gdacs` |
| ReliefWeb 危機 proxy | 5 分鐘 cache | `/api/risk/crises` |

> 現況：`risk_events.json` 檔案存在，但目前未在 `app.py` 被載入成靜態事件來源。

### 事件來源詳表

| 類別 | 來源 | 顯示 / 落地規則 |
|------|------|----------------|
| 地震 | USGS FDSN query API | proxy 抓過去 56 天 M4.5+，5 分鐘 cache；Supabase 落地 loop 抓 day feed 並寫 M5+ |
| 氣旋/颱風 | NOAA NHC `CurrentStorms.json` | proxy 5 分鐘 cache；落地 loop 僅寫 96kt+ |
| 洪水/火山 | GDACS API | Orange/Red，僅 3 天內；TC 不從 GDACS 抓，避免和 NOAA 重複 |
| ReliefWeb 危機 | ReliefWeb disasters API | ongoing disasters，5 分鐘 cache，供前端危機 layer 使用 |
| 罷工 | Bing News RSS | 8 個目標公司，過去 56 天，主語驗證後落地 |
| 地緣政治/戰爭 | Bing News RSS | 7 個預設風險地區，過去 56 天 |
| 操作異常 / 財務風險 | 新聞快取關鍵字偵測 | `operational` 會計入分數；`financial` 只顯示不計分 |

### 地震規模分層（重要！四個獨立門檻）

USGS 抓資料、DB 落地、網站顯示、Telegram 推播是**四個獨立的門檻**，不可混淆：

| 階段 | 門檻 | 程式位置 |
|------|------|---------|
| ① USGS API 抓取 | M ≥ 4.5（多抓做 buffer）| `app.py` `/api/risk/quakes` |
| ② Supabase 落地 / Telegram 推播池 | M ≥ 5.0 | `app.py` `_fetch_usgs_quakes` |
| ③ **網站地圖 / 事件清單顯示** | **M ≥ 6.5（台灣 CWA 紅燈級）** | `templates/index.html` `_loadQuakes` |
| ④ 供應商紅綠燈影響期 | 同 ③ + **3 天觀察期** | `templates/index.html` `_evalSupplierRisk` |

**地圖只顯示 M6.5+ 的兩級燈號：**

| Impact | 規模 | 地圖顏色 | 對應台灣 CWA 標準 |
|--------|------|----------|-----------------|
| CRITICAL | M ≥ 7.0 | 🔴 紅 | 紅燈 / 規模 7+ |
| HIGH | M 6.5 ~ 6.9 | 🟠 橙 | 紅燈 / 規模 6 弱+ |

> 為什麼不顯示 M5-6.4？全球 M5+ 一年 ~1500 筆，地圖會擠到無法閱讀；M6.5+ 一年 ~80 筆，乾淨且都是真正影響供應鏈的等級。

**Telegram 推播 min_severity 對應到實際規模：**

| min_severity | 收到的最低規模 |
|--------------|--------------|
| low | M5.0+ |
| medium | M5.5+ |
| high | M6.0+ |
| critical（隱含）| M6.5+ |

### 事件保留 window（顯示期 vs 供應商影響期）

| 事件類型 | 地圖顯示期 | 供應商紅綠燈影響期 |
|---------|-----------|-----------------|
| 地震 | **8 週**（USGS query 56 天）| **3 天觀察期** |
| 颱風 | NOAA active storm 期間 | 同左 |
| 洪水/火山 | GDACS 3 天內 | 同左 |
| 罷工 | 過去 8 週新聞 | 同左 |
| 地緣政治/戰爭 | 過去 8 週新聞 | 同左 |

> 為什麼地震分兩個 window？4 週前的宜蘭地震還在地圖上讓你回顧，但不該繼續把附近供應商標紅 — 評估期過了就要解除。

### 罷工監控目標公司

三星電子、富士康、SK海力士、LG、比亞迪、台積電、聯發科、UPS

### 地緣政治監控項目

紅海航運威脅、台灣海峽地緣緊張、伊朗地區衝突、俄烏戰爭、剛果礦產衝突、緬甸內戰、印巴邊境緊張。

### 主語驗證機制

避免「事件被錯誤歸屬給只是被順帶提及的公司」：

1. 公司 alias 主語驗證。
2. 標題/摘要必須有罷工、工潮、walkout 等動作詞。
3. 排除法律糾紛、罰款、專利、strike price、deal/agreement 等非勞資事件。
4. Bing 429 時退避重試，geo/strike scan 各限制最多 2 worker。

### 風險指數計算

`/api/risk` 會將新聞快取、罷工 cache、地緣 cache 合併計分。

| 類型 | 基礎權重 |
|------|----------|
| 災害 | 30 |
| 地緣政治 | 20 |
| 罷工 | 20 |
| 操作異常 | 15 |
| 財務 | 0，不計分 |

修正規則：

- 7 天內 x 1.0；第 8 天起每日遞減 10%，最低 0.3。
- 超過 21 天不計分。
- 罷工/地緣政治未確認 x 0.6。
- 罷工/操作異常非長期事件 x 0.7。
- 災害新聞牆只顯示 3 天內且有嚴重影響字眼的颱風/洪水類新聞。

警示閾值：

- 紅色：>= 60，立即關注。
- 黃色：>= 28，持續監控。
- 綠色：< 28，正常。

---

## 4. Telegram Bot 推播

### Bot 資訊

| 項目 | 值 |
|------|---|
| Username | `@Lalalaralara_bot` |
| 連結 | `https://t.me/Lalalaralara_bot` |
| 目前啟動模式 | Polling |

目前 `app.py` 的規則是：

- Render：`RENDER=true` 時 module-load 會啟動災害偵測與 Telegram polling。
- 本地：預設不跑 polling，除非設定 `TELEGRAM_FORCE_LOCAL_POLLING=true`。
- 未設定 `TELEGRAM_BOT_TOKEN`：skip polling。

### 更新與推播頻率

| 機制 | 頻率 | 說明 |
|------|------|------|
| 訂閱命中掃描 | **每 30 秒**，first=10 秒 | PTB `job_queue.run_repeating(... interval=30 ...)` |
| 每批處理 | 50 件 pending events | `dispatch_pending(batch=50)` |
| 事件落地 | geo/strike scan 後 async 寫入；災害 loop 每 90 秒寫入 | Supabase `risk_events` |
| 推播限速 | 預設 25 訊息/秒，可用 `TELEGRAM_RATE_LIMIT_PER_SEC` 調整 | Telegram 全域上限 30/s，留 buffer |
| 同事件對同人 | 永遠只推 1 次 | `notification_log` UNIQUE 約束 |

### 訂閱類型

| 類型 | 範例 | 命中邏輯 |
|------|------|---------|
| 地區 | 台灣、日本、台灣/新竹 | 訂閱地區與事件 region 子字串雙向比對 |
| 料件類別 | BATTERY、IC、MEMORY 等 | 事件地區內 supplier 是否含該 part_category |
| 供應商分布 | 從 `suppliers.json` 同步的 22 筆地區聚合 | supplier.region 與 event.region 雙向比對 |
| 半徑 | lat/lng/km | Haversine 距離 <= km |

地震另有距離過濾：依 impact 使用 80/150/300/500 km 半徑，避免遠距城市被地震事件誤推。

### 推播新鮮度與嚴重等級

| 規則 | 現況 |
|------|------|
| 地震 | 發生 30 分鐘內才推 |
| 其他事件 | 發生或入庫 24 小時內才推 |
| 沒時間資訊 | 保守放行 |
| 訂閱最低等級 | low / medium / high |
| 事件 impact | LOW / MED / HIGH / CRITICAL |
| 低風險事件 | `disable_notification=True` |

### Supabase 資料表

`telegram_users` / `subscriptions` / `notification_log` / `risk_events` / `suppliers` / `parts` / `supplier_parts` / `wizard_state`

`suppliers.json` 目前 22 筆，Render 啟動時會同步到 Supabase `suppliers` 表。

---

## 5. 系統架構與部署

### 後端

| 項目 | 值 |
|------|---|
| Framework | Flask 3.x |
| Python | 3.10 |
| WSGI | gunicorn |
| Telegram | python-telegram-bot 21+ / 22.x 相容，使用 job queue |
| DB | Supabase PostgreSQL，主要供 Telegram bot 使用 |
| 套件管理 | `requirements.txt` |

### 前端

- 純 vanilla JS。
- 圖表：Chart.js + chartjs-plugin-annotation。
- 地圖：Leaflet.js + leaflet.heat。
- CSS：自訂樣式與 utility class。

### 部署設定

| 檔案 | 目前設定 |
|------|----------|
| `render.yaml` | `gunicorn --workers 1 --threads 4 --timeout 180 app:app` |
| `Procfile` | `gunicorn --workers 1 --threads 4 --timeout 180 app:app` |
| `railway.json` | `gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120` |

正式站 URL 仍記錄為：

```text
https://asustimes.onrender.com
```

本地啟動：

```bash
python app.py
# http://localhost:5050
```

### 背景執行緒

| 名稱 | 頻率 / 行為 | 用途 |
|------|-------------|------|
| `background_refresh_loop` | 啟動立即一次，之後每 1 小時 | 新聞 |
| `_live_price_loop` | 啟動立即一次，之後台北 07/09/11/13/15/17 | 商品價格 |
| `_risk_cache_preload_loop` | 啟動立即一輪，之後每 3 小時 | 罷工 + 地緣政治 |
| `_disaster_persist_loop` | 首輪 30 秒後，之後每 90 秒 | USGS/NOAA/GDACS 落地 Supabase |
| `daily_digest_loop` | 每 60 秒檢查一次 `DIGEST_HOUR` UTC | email digest |
| `_telegram_bot_loop` | 持續 polling | Telegram bot |

> 注意：目前 `@app.before_request` 與 `/` route 內的 `ensure_background_threads()` 都會啟動部分背景 thread，兩者使用不同 flag。報告按現況記錄；若要避免重複 loop，需另外調整程式啟動邏輯。

### 工具腳本

| 腳本 | 用途 |
|------|------|
| `tools/run_migrations.py` | 跑 Supabase migration |
| `tools/seed_suppliers.py` | 從 `suppliers.json` 同步到 Supabase |
| `tools/simulate_risk_event.py` | 模擬風險事件、測試 pending/dispatch |
| `merge_excel_history.py` | 從 Excel 匯入歷史價格 |
| `apply_carry_forward.py` | 對價格 CSV 補 carry-forward |

---

## 最近變更紀錄

### 2026-05-05

- 將新聞主刷新頻率更新為每 1 小時。
- 移除報告中已不存在的 Digitimes 2 小時獨立刷新與 GitHub Action 商品備援。
- 將新聞來源改為現況：Digitimes 官方 RSS 22 組 + Bing News site/keyword 搜尋。
- 將工商時報改列為 Bing News site 搜尋，不再列直接 RSS。
- 將原物料來源更新為 Yahoo Finance / cnyes / Trading Economics / SCI99 / BOT / buyplas / MoneyDJ / SMM 現況。
- 將商品總數更新為 26 項，匯率更新為 8 種。
- 補上 Commodity News 的 mining.com / GDELT / Bing News 查詢與節流規則。
- 將風險事件更新為 USGS 56 天 proxy、災害每 90 秒落地、ReliefWeb 5 分鐘 cache。
- 將 Telegram dispatcher 更新為每 30 秒掃描 pending events。
- 記錄目前背景 thread 可能由兩個入口啟動的現況。

### 2026-04-29

- Telegram Bot M1-M3：訂閱精靈、4 種訂閱類型、自動推播、事件去重。
- Supabase 資料庫整合：8 張表，supplier 從 JSON 同步。
- 新鮮度過濾：地震 30 分鐘、其他 24 小時內才推。
- Yahoo Finance v8 fallback：金/銀/油/匯率可不依賴 yfinance lib。
- Carry-forward 機制：CSV 空白沿用最近真實值並標 `*`。
- SCI99 JSON API：黃磷 + PC 改走 AJAX JSON 端點。

### 2026-04-28

- 整合使用說明到網頁使用指南頁。
- 風險指數計算公式加進開發者指南。
- 罷工/地緣政治改用 8 週 publish date 過濾。

---

*欲查看更詳細的程式邏輯、命中演算法與資料庫 schema，請見 `DEVELOPER_GUIDE.md`。*
